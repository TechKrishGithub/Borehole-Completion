import { Modal } from "react-native-paper";
import {View,Text,Button} from 'react-native';
import { Alert } from "react-native";

const LogOutOptions=(props)=>{
    return(
    <View>
        
    </View>
    )
}

export default LogOutOptions;